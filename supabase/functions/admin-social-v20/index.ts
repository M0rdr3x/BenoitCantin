import {corsHeaders,json} from '../_shared/cors.ts';import {requiredUser,serviceClient} from '../_shared/auth.ts';
async function ctx(req:Request){const user=await requiredUser(req),s=serviceClient();const {data}=await s.rpc('is_sinjira_admin',{p_user_id:user.id});if(!data)throw new Error('ADMIN_REQUIRED');return {user,s}}
async function reportTargetUser(s:any,r:any){const snap=r.snapshot||{};if(snap.user_id)return snap.user_id;if(snap.sender_user_id)return snap.sender_user_id;if(snap.character_id){const {data}=await s.from('character_social_profiles').select('user_id').eq('character_id',snap.character_id).maybeSingle();return data?.user_id||null}if(snap.sender_character_id){const {data}=await s.from('character_social_profiles').select('user_id').eq('character_id',snap.sender_character_id).maybeSingle();return data?.user_id||null}if(r.network==='real'&&r.target_type==='post'){const {data}=await s.from('social_real_posts').select('user_id').eq('id',r.target_id).maybeSingle();return data?.user_id||null}if(r.network==='character'&&r.target_type==='post'){const {data}=await s.from('social_character_posts').select('user_id').eq('id',r.target_id).maybeSingle();return data?.user_id||null}return null}
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});if(req.method!=='POST')return json({ok:false,error:'Méthode non autorisée.'},405);try{const {user,s}=await ctx(req),b=await req.json(),a=String(b.action||'');if(a==='dashboard'){const [{count:open},{count:susp}]=await Promise.all([s.from('social_reports').select('id',{count:'exact',head:true}).eq('status','open'),s.from('social_suspensions').select('user_id',{count:'exact',head:true}).or(`until_at.is.null,until_at.gt.${new Date().toISOString()}`)]);return json({ok:true,dashboard:{open_reports:open||0,active_suspensions:susp||0}})}if(a==='list_reports'){const {data,error}=await s.from('social_reports').select('*').eq('status','open').order('created_at',{ascending:false}).limit(300);if(error)throw error;return json({ok:true,reports:data||[]})}if(a==='resolve_report'){const {error}=await s.from('social_reports').update({status:'resolved',reviewed_at:new Date().toISOString(),reviewed_by:user.id}).eq('id',b.report_id);if(error)throw error;return json({ok:true})}if(a==='remove_reported_content'){const {data:r,error}=await s.from('social_reports').select('*').eq('id',b.report_id).single();if(error)throw error;const map:any={real:{post:'social_real_posts',comment:'social_real_comments',message:'social_real_messages'},character:{post:'social_character_posts',comment:'social_character_comments',message:'social_character_messages'}};const table=map?.[r.network]?.[r.target_type];if(table){const {error:e}=await s.from(table).delete().eq('id',r.target_id);if(e)throw e}await s.from('social_reports').update({status:'resolved',reviewed_at:new Date().toISOString(),reviewed_by:user.id}).eq('id',r.id);return json({ok:true})}if(a==='suspend_reported_user'){const {data:r,error}=await s.from('social_reports').select('*').eq('id',b.report_id).single();if(error)throw error;const uid=await reportTargetUser(s,r);if(!uid)throw new Error('TARGET_USER_NOT_FOUND');const days=Math.max(1,Math.min(Number(b.days||7),365));const until=new Date(Date.now()+days*86400000).toISOString();const {error:e}=await s.from('social_suspensions').upsert({user_id:uid,reason:`Signalement ${r.id}: ${r.reason}`,until_at:until,created_by:user.id},{onConflict:'user_id'});if(e)throw e;return json({ok:true,until_at:until})}return json({ok:false,error:'Action inconnue.'},400)}catch(e){console.error(e);if(e?.message==='AUTH_REQUIRED')return json({ok:false,error:'Connexion requise.'},401);if(e?.message==='ADMIN_REQUIRED')return json({ok:false,error:'Administration refusée.'},403);return json({ok:false,error:e?.message||'Erreur de modération sociale.'},500)}});