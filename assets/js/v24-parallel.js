import {getSupabase,requireUser,setStatus,escapeHtml,isSinjiraOwner} from './sinjira-supabase.js';
const missionBox=document.querySelector('[data-parallel-mission]'),stateBox=document.querySelector('[data-parallel-state]'),status=document.querySelector('[data-parallel-status]');
function empty(msg){return `<div class="v24-empty">${escapeHtml(msg)}</div>`}
if(missionBox&&stateBox){try{const user=await requireUser();const s=getSupabase();const {data:character,error:ce}=await s.from('characters').select('id,public_name,status,visible_to_user').eq('user_id',user.id).limit(1).maybeSingle();if(ce)throw ce;if(!character){missionBox.innerHTML=empty(isSinjiraOwner(user)?'Le personnage AbyssTime doit être resynchronisé par le correctif Supabase V24.1.':'Votre personnage doit d’abord être créé ou approuvé.');stateBox.innerHTML=empty('Aucune Chronique personnelle pour le moment.');}else{const [{data:missions,error:me},{data:state,error:se}]=await Promise.all([s.from('parallel_missions').select('id,title,prompt,cycle_id,closes_at,parallel_cycles(title,status)').eq('status','open').order('created_at',{ascending:false}).limit(1),s.from('parallel_character_state').select('*').eq('character_id',character.id).maybeSingle()]);if(me||se)throw me||se;const rows=Array.isArray(missions)?missions:[];const m=rows[0];missionBox.innerHTML=m?`<article><span class="v24-badge live">${escapeHtml(m.parallel_cycles?.title||'Cycle actif')}</span><h3>${escapeHtml(m.title)}</h3><p>${escapeHtml(m.prompt)}</p><form data-parallel-response-form><label>Votre réponse<textarea name="response" maxlength="4000" required></textarea></label><button class="btn btn-primary" type="submit">Enregistrer ma réponse</button></form></article>`:empty('Aucune mission ouverte actuellement.');stateBox.innerHTML=state?`<p><strong>${escapeHtml(character.public_name||'Mon personnage')}</strong></p><p>Dernière mise à jour : ${escapeHtml(state.updated_at||'—')}</p><p>${escapeHtml(state.private_summary||'Aucun résumé privé encore enregistré.')}</p>`:empty('La Chronique sera créée lors de votre première participation.');const responseForm=missionBox.querySelector('[data-parallel-response-form]');responseForm?.addEventListener('submit',async e=>{e.preventDefault();const response=String(new FormData(responseForm).get('response')||'').trim();const {error}=await s.from('parallel_responses').upsert({mission_id:m.id,user_id:user.id,character_id:character.id,response_text:response},{onConflict:'mission_id,user_id'});setStatus(status,error?error.message:'Réponse enregistrée. Elle reste liée à ce cycle et à votre personnage.',error?'error':'success')});}}catch(e){missionBox.innerHTML=empty('Le module Monde parallèle attend le correctif Supabase V24.1.');stateBox.innerHTML=empty('Aucune donnée chargée.');setStatus(status,e.message||'Monde parallèle indisponible.','error')}}
