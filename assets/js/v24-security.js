import {getSupabase,requireUser,escapeHtml,setStatus} from './sinjira-supabase.js';
const sessionBox=document.querySelector('[data-v24-security-session]'),mfaBox=document.querySelector('[data-v24-security-mfa]'),status=document.querySelector('[data-v24-security-status]');
try{const user=await requireUser();const s=getSupabase();const {data:{session}}=await s.auth.getSession();if(sessionBox)sessionBox.innerHTML=`<p><strong>${escapeHtml(user.email||'Compte')}</strong></p><p>Courriel : ${user.email_confirmed_at?'vérifié':'à confirmer'}</p><p>Session active : ${session?'oui':'non'}</p>`;try{const {data,error}=await s.auth.mfa.listFactors();if(error)throw error;const count=[...(data?.totp||[]),...(data?.phone||[])].filter(x=>x.status==='verified').length;mfaBox.innerHTML=count?`<p><strong>${count} facteur(s) MFA vérifié(s)</strong></p>`:'<p>Aucun facteur MFA vérifié pour le moment.</p>';}catch{mfaBox.textContent='État MFA non disponible dans cette configuration.'}}catch(e){setStatus(status,e.message||'Impossible de lire l’état de sécurité.','error')}
