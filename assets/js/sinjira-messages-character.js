import {getSupabase,requireCommunityUser,escapeHtml,formatDate,reportContent,socialStatus} from './sinjira-social-common.js';
const contacts=document.querySelector('[data-contact-list]'),search=document.querySelector('[data-contact-search]'),log=document.querySelector('[data-chat-log]'),form=document.querySelector('[data-chat-form]'),title=document.querySelector('[data-chat-title]'),status=document.querySelector('[data-social-status]'),lock=document.querySelector('[data-character-message-lock]');let user,me,all=[],peer=null;
const portrait=()=>'/assets/media/sinjira-emblem.webp';
async function loadContacts(){const {data=[]}=await getSupabase().from('character_social_profiles').select('*').neq('user_id',user.id).order('public_name');all=data;renderContacts()}
function renderContacts(){const q=(search?.value||'').toLowerCase();contacts.innerHTML=all.filter(x=>(x.public_name||'').toLowerCase().includes(q)).map(x=>`<button class="v20-contact character" data-character="${x.character_id}"><img src="${portrait()}" alt=""><span><strong>${escapeHtml(x.public_name||'Personnage')}</strong><small>Personnage SINJIRA</small></span></button>`).join('')||'<p>Aucun personnage disponible.</p>';contacts.querySelectorAll('[data-character]').forEach(b=>b.addEventListener('click',()=>openPeer(b.dataset.character)))}
async function openPeer(id){peer=all.find(x=>x.character_id===id);if(!peer)return;history.replaceState({},'',`?character=${encodeURIComponent(id)}`);title.innerHTML=`<div class="v20-social-identity"><img class="v20-social-avatar character" src="${portrait()}" alt=""><div><strong>${escapeHtml(peer.public_name)}</strong><span class="v20-social-time">Conversation personnage · rôle-play</span></div></div>`;form.hidden=false;await messages()}
async function messages(){const s=getSupabase();const {data=[],error}=await s.from('social_character_messages').select('*').or(`and(sender_character_id.eq.${me.character_id},recipient_character_id.eq.${peer.character_id}),and(sender_character_id.eq.${peer.character_id},recipient_character_id.eq.${me.character_id})`).order('created_at');if(error)throw error;log.innerHTML=data.map(m=>`<div class="v20-bubble character ${m.sender_user_id===user.id?'mine':''}" data-message="${m.id}">${escapeHtml(m.body)}<time>${escapeHtml(formatDate(m.created_at))}</time>${m.sender_user_id!==user.id?'<button class="link-button" data-report-message>Signaler</button>':''}</div>`).join('')||'<p>Commencez le rôle-play. Les échanges ici ne modifient pas le canon officiel.</p>';log.scrollTop=log.scrollHeight;log.querySelectorAll('[data-report-message]').forEach(b=>b.addEventListener('click',async()=>{const el=b.closest('[data-message]'),m=data.find(x=>x.id===el.dataset.message),reason=prompt('Pourquoi signalez-vous ce message de rôle-play?');if(!reason)return;await reportContent({network:'character',target_type:'message',target_id:m.id,reason,snapshot:{body:m.body,sender_character_id:m.sender_character_id,recipient_character_id:m.recipient_character_id}});alert('Message signalé à l’administration.')}))}
(async()=>{try{user=await requireCommunityUser();const {data}=await getSupabase().from('character_social_profiles').select('*').eq('user_id',user.id).maybeSingle();me=data;if(!me){document.querySelector('.v20-message-shell').hidden=true;lock.hidden=false;lock.innerHTML='<div class="v20-character-lock"><h2>Messagerie personnage verrouillée</h2><p>Votre unique personnage doit d’abord être approuvé et visible.</p><a class="btn btn-primary" href="mon-personnage.html">Voir Mon personnage</a></div>';return}await loadContacts();search?.addEventListener('input',renderContacts);form.addEventListener('submit',async e=>{e.preventDefault();if(!peer)return;const body=String(new FormData(form).get('body')||'').trim();if(!body)return;const {error}=await getSupabase().from('social_character_messages').insert({sender_user_id:user.id,recipient_user_id:peer.user_id,sender_character_id:me.character_id,recipient_character_id:peer.character_id,body});if(error)return socialStatus(status,error.message,'error');form.reset();await messages()});getSupabase().channel(`character-messages-${user.id}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'social_character_messages'},payload=>{const m=payload.new;if(peer&&((m.sender_character_id===me.character_id&&m.recipient_character_id===peer.character_id)||(m.sender_character_id===peer.character_id&&m.recipient_character_id===me.character_id)))messages()}).subscribe();const q=new URLSearchParams(location.search).get('character');if(q)await openPeer(q)}catch(e){if(e?.message!=='RULES_REQUIRED'&&e?.message!=='Connexion requise')socialStatus(status,e.message||'Erreur de messagerie personnage.','error')}})();