import {getSupabase,requireUser,setStatus,escapeHtml,isSinjiraOwner} from './sinjira-supabase.js';
const form=document.querySelector('[data-license-redeem-form]'),status=document.querySelector('[data-license-status]'),list=document.querySelector('[data-license-list]');
if(form&&list){const user=await requireUser();const s=getSupabase();const owner=isSinjiraOwner(user);async function render(){if(owner){const {data,error}=await s.from('products').select('slug,name,product_type,active').eq('active',true).order('name');if(error){list.innerHTML='<div class="v24-empty">Impossible de lire le catalogue.</div>';return}const products=Array.isArray(data)?data:[];list.innerHTML='<article class="v24-panel v24-owner-access"><span class="v24-badge live">Compte propriétaire</span><h3>Accès total SINJIRA™</h3><p>Romans, jeux, tests et produits numériques SINJIRA™ sont débloqués pour AbyssTime. Aucun code d’activation n’est requis.</p></article>'+products.map(x=>`<article class="v24-panel"><span class="v24-badge live">Accès propriétaire</span><h3>${escapeHtml(x.name||x.slug||'Produit SINJIRA™')}</h3><p>${escapeHtml(x.product_type||'contenu')}</p></article>`).join('');form.hidden=true;return}const {data,error}=await s.from('user_entitlements').select('source,granted_at,products(slug,name,product_type)').eq('user_id',user.id).order('granted_at',{ascending:false});if(error){list.innerHTML='<div class="v24-empty">Impossible de lire les licences.</div>';return}const rows=Array.isArray(data)?data:[];list.innerHTML=rows.length?rows.map(x=>`<article class="v24-panel"><span class="v24-badge live">Possédé</span><h3>${escapeHtml(x.products?.name||x.products?.slug||'Produit SINJIRA™')}</h3><p>Source : ${escapeHtml(x.source||'licence')}</p></article>`).join(''):'<div class="v24-empty">Aucune licence liée à ce compte.</div>';}form.addEventListener('submit',async e=>{e.preventDefault();if(owner)return;const code=String(new FormData(form).get('code')||'').trim();const {data,error}=await s.functions.invoke('redeem-license-code',{body:{code}});if(error||!data?.ok){setStatus(status,data?.error||error?.message||'Activation impossible.','error');return}form.reset();setStatus(status,'Produit activé sur votre Compte SINJIRA™.','success');render()});await render();}
