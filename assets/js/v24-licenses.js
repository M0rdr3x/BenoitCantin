import {getSupabase,requireUser,setStatus,escapeHtml} from './sinjira-supabase.js';
const form=document.querySelector('[data-license-redeem-form]'),status=document.querySelector('[data-license-status]'),list=document.querySelector('[data-license-list]');
if(form&&list){const user=await requireUser();const s=getSupabase();async function render(){const {data,error}=await s.from('user_entitlements').select('source,granted_at,products(slug,name,product_type)').eq('user_id',user.id).order('granted_at',{ascending:false});if(error){list.innerHTML='<div class="v24-empty">Impossible de lire les licences.</div>';return}list.innerHTML=(data||[]).length?(data||[]).map(x=>`<article class="v24-panel"><span class="v24-badge live">Possédé</span><h3>${escapeHtml(x.products?.name||x.products?.slug||'Produit SINJIRA™')}</h3><p>Source : ${escapeHtml(x.source||'licence')}</p></article>`).join(''):'<div class="v24-empty">Aucune licence liée à ce compte.</div>';}form.addEventListener('submit',async e=>{e.preventDefault();const code=String(new FormData(form).get('code')||'').trim();const {data,error}=await s.functions.invoke('redeem-license-code',{body:{code}});if(error||!data?.ok){setStatus(status,data?.error||error?.message||'Activation impossible.','error');return}form.reset();setStatus(status,'Produit activé sur votre Compte SINJIRA™.','success');render()});await render();}
