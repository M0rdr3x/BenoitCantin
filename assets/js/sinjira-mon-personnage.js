import {getSupabase,requireUser,escapeHtml,formatDate} from './sinjira-supabase.js';const box=document.querySelector('[data-my-character]'),status=document.querySelector('[data-character-status]');const labels={submitted:'Questionnaire reçu',ai_draft:'Brouillon',author_review:'En préparation',approved:'Approuvé',assigned:'Roman attribué',future:'Futur roman',published:'Publié',refused:'Refusé',archived:'Archivé'};(async()=>{try{const user=await requireUser('/compte/connexion.html'),s=getSupabase();const [{data:sub},{data:ch},{data:events=[]}]=await Promise.all([s.from('character_submissions').select('*').eq('user_id',user.id).order('created_at',{ascending:false}).limit(1).maybeSingle(),s.from('characters').select('id,submission_id,public_name,public_description,status,novel_note,visible_to_user,novels(title)').eq('user_id',user.id).limit(1).maybeSingle(),s.from('character_status_events').select('*').eq('user_id',user.id).order('created_at')]);if(ch){const ready=['approved','assigned','future','published'].includes(ch.status)&&ch.visible_to_user;box.innerHTML=`<article class="character-account-card"><div class="v20-status-line"><span class="v20-status-pill ${ready?'ready':'wait'}">${escapeHtml(labels[ch.status]||ch.status)}</span><span>1 personnage maximum par Compte SINJIRA</span></div><h1>${escapeHtml(ch.public_name||'Personnage SINJIRA')}</h1><p>${escapeHtml(ch.public_description||'La description officielle sera ajoutée par Benoit Cantin.')}</p><div class="character-bible"><div><strong>Roman</strong><br>${escapeHtml(ch.novels?.title||ch.novel_note||'À attribuer / futur roman')}</div></div>${events.length?`<div class="v19-timeline">${events.map(e=>`<div class="v19-timeline-item"><strong>${escapeHtml(labels[e.status]||e.status)}</strong><span>${escapeHtml(formatDate(e.created_at))}${e.note?` · ${escapeHtml(e.note)}`:''}</span></div>`).join('')}</div>`:''}${ready?'<div class="hero-actions"><a class="btn btn-primary" href="reseau-personnage.html">Entrer dans le Réseau personnage</a><a class="btn btn-secondary" href="messages-personnage.html">Messages personnage</a></div>':'<div class="v20-character-lock"><p>Le Réseau personnage sera disponible lorsque votre personnage sera approuvé et prêt à être utilisé.</p></div>'}</article>`;return}if(sub){box.innerHTML=`<div class="v20-character-lock"><span class="v20-status-pill wait">${escapeHtml(labels[sub.status]||sub.status)}</span><h2>Votre demande est déjà enregistrée</h2><p>Un Compte SINJIRA ne peut avoir qu’un seul personnage. Le questionnaire est maintenant verrouillé pour ce compte pendant la préparation.</p><a class="btn btn-secondary" href="../projets/sinjira/registre/">Voir le Registre</a></div>`;return}box.innerHTML='<div class="v20-character-ready"><h2>Vous n’avez pas encore de personnage</h2><p>Votre Compte SINJIRA peut recevoir un seul personnage officiel issu du Registre des Consciences.</p><a class="btn btn-primary" href="../projets/sinjira/registre/">Remplir le questionnaire</a></div>'}catch(e){if(e?.message!=='Connexion requise'){status.textContent=e.message||'Erreur.';status.hidden=false}}})();