import {getSupabase,escapeHtml} from './sinjira-supabase.js';async function call(action,extra={}){const {data,error}=await getSupabase().functions.invoke('admin-social-v20',{body:{action,...extra}});if(error||!data?.ok)throw new Error(data?.error||error?.message||'Erreur modération sociale');return data}async function load(){const d=await call('dashboard');document.querySelector('[data-social-report-count]').textContent=String(d.dashboard.open_reports||0);document.querySelector('[data-social-suspension-count]').textContent=String(d.dashboard.active_suspensions||0);const r=await call('list_reports'),box=document.querySelector('[data-social-report-list]');box.innerHTML=(r.reports||[]).map(x=>`<article class="v20-admin-report" data-report="${x.id}"><strong>${escapeHtml(x.network)} · ${escapeHtml(x.target_type)} · ${escapeHtml(x.reason)}</strong><p>Signalé le ${escapeHtml(new Date(x.created_at).toLocaleString('fr-CA'))}</p><pre>${escapeHtml(JSON.stringify(x.snapshot||{},null,2))}</pre><div class="v20-admin-actions"><button class="btn btn-primary btn-small" data-resolve>Résoudre</button><button class="btn btn-secondary btn-small" data-remove>Retirer le contenu signalé</button><button class="btn btn-secondary btn-small" data-suspend>Suspension 7 jours</button></div></article>`).join('')||'<p>Aucun signalement ouvert.</p>';box.querySelectorAll('[data-report]').forEach(card=>{const id=card.dataset.report;card.querySelector('[data-resolve]').onclick=async()=>{await call('resolve_report',{report_id:id});load()};card.querySelector('[data-remove]').onclick=async()=>{if(confirm('Retirer le contenu signalé lorsque cela est techniquement possible?')){await call('remove_reported_content',{report_id:id});load()}};card.querySelector('[data-suspend]').onclick=async()=>{if(confirm('Suspendre socialement le compte visé pendant 7 jours?')){await call('suspend_reported_user',{report_id:id,days:7});load()}}})}(async()=>{try{await load()}catch(e){console.error(e)}})();